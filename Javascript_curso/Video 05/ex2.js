// retorne no console apenas as imagens que comecam com a palavra imagem 

const imagens = document.querySelectorAll("img")

// retorne no console apenas as imagens que começaram com a palavra imagem

const imagensComeca = document.querySelectorAll('img[scr^="img/imagem"]')

// selecione todos os links internos (onde o href começa com #)
const linkInterno = documet.querySelectorAll('[href^="#"]')

// selecione o primeiro h2 dentro de .animais-descricao

const h2Animais = document.querySelector('.animais-descricao h2')

// selecione o ultimo p do site

const ultimoP = document.querySelectorAll('p')
console.log(ultimoP[paragrafos.length - 1])

